using UnityEngine;

public class PersistentCard : CardBase
{
    public override void PlayCard(CardPlayPresenter presenter , Parameters parameters)
    {
        // 処理はここに記述
        // presenter.StartPersistent(_cardData.keepTurns);
    }
}